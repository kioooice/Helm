# Helm 工作约定

## 先理解任务

阅读顺序：README.md → docs/current-state.md → PLAN.md 中当前阶段 → docs/acceptance.md。开始前读取最新分支与实际源码，记录工作基线，不能把旧文档或历史测试结果当作当前状态。

当前用户分工：规划助手负责方向、任务拆解和阶段验收；执行模型负责实现、自测、修复和交接；用户判断个人价值及目标取舍。模型名称不决定权限。执行模型的自测不等于独立验收。

用户的新明确指令优先于这些工作约定。2026-09-06 的任务只更新计划和说明；路线中的代码任务需要后续明确分配，不能据此自动实施全部阶段。

## 执行边界

- 一次完成一个已分配任务包，包含必要的局部修复、自测和可复查交接。常规实现选择自主处理，不逐文件询问。
- 不擅自扩大为通用 Agent 平台、多源采集系统、目标管理系统或完整行为教练。
- 不把推测写成事实，不虚构对比基线、用户目标或持续时长，不用诊断性标签代替证据。
- 不通过降低验收标准、删去失败案例、修改测试预期来制造通过。
- 如必须改变对外接口、数据保留/外发政策、目标语义或验收定义，整理原因、方案、影响及推荐项，交规划助手决策；先完成不受影响的已授权工作。
- 不因文档列出未来数据源，就采集新来源或向模型服务上传真实记录。只使用任务明确允许的数据与处理方式。不得提交个人日志、截图、密钥或数据库。
- 不删除用户数据、不做破坏性迁移来解决局部开发问题。涉及迁移须说明兼容性与回退方法。

## 完成与交接

每次交接写清：任务 ID、基线和结果提交、改动目的、对应验收条目、实际执行的命令及结果、运行环境、未验证内容、已知限制、复现步骤和下一步建议。

测试被环境阻断就记录阻断，不能记为通过。界面截图不能替代数据流验证；模拟模型响应不能证明真实洞察质量。用户是否觉得有用，必须由用户反馈，不能由另一个模型代答。

实现类任务补充能检出该风险的测试；文档改动检查事实、链接和阶段一致性即可。验证范围由具体风险决定，不为小改动堆积测试。

规划助手的阶段结论必须明确为：通过／有条件通过（列出条件与允许范围）／返工／暂停。尚未验收时写“待验收”，不自行进入下一产品阶段。
