# 当前实现说明

记录日期：2026-09-06。源码基线：T0 交接提交（见 [交接记录](handoffs/T0-2026-09-06.md)），基于文档版基线 `a77be2d` 演进。本轮为 T0 任务包的代码实现，验证命令与结果见文末。

## 已有能力与未完成部分

| 环节 | 已有实现 | 不能据此宣称 |
| --- | --- | --- |
| 采集 | 定时屏幕采集、本地 OCR、指纹去重、手动控制 | 完整记录一天、理解用户意图 |
| 存储 | raw_captures 与事件、报告、目标、基线相关表/API | 已生成结构化行为事件、目标闭环已完成 |
| 报告 | 手动触发 + **发送前本地预览（零网络）** + 用户确认后才请求模型；输出附带覆盖元数据；提示词禁用无依据比较 | 自动定时推送、可靠纵向比较、洞察质量已验证 |
| 界面 | 报告展示（按报告所属日期标注）、发送前确认与取消、覆盖与截断说明、模型配置、采集及暂停操作 | 证据定位、纠错后重新分析、追问已经完成 |
| 清理 | 默认 12 小时保留参数，启动和特定采集路径清理 | 每份文件严格在 12 小时删除、不可恢复擦除 |

## 语义差距的处理状态（对应 2026-09-06 文档版列出的五项）

1. **实际外发内容**：已实现发送前预览确认——预览阶段不发任何网络请求，界面明确显示将发送的条数、覆盖时间范围、目标服务地址，并声明"原始 OCR 内容未经脱敏"。取消则一次请求都不发生。输入内容本身的脱敏仍未实现（架构上留待 V1）。
2. **没有可用历史对比输入**：系统提示词已改为"未提供历史基线与目标，禁止比较性或趋势性表述，证据不足时明确写证据不足，允许零条发现"。接入 baselines/goals 仍属 V1。
3. **覆盖与截断**：payload 的 captureCount / chunkCount / truncated / coveredFrom / coveredTo 已随预览与报告结果返回，并在界面展示。**已知保留的决策**：截断时仍取最早的 120 条（沿用既有行为），"取最近还是取均匀样本"待规划助手决定；去重快照数不换算时长的原则未变。
4. **证据与纠正断开**：本轮未处理，属 V1 范围。
5. **清理保证有限**：机制未改（按约定不改保留政策）。仅将界面与文档口径统一为"采集运行期间按 12 小时参数滚动清理（非严格定时删除）"。

相关入口：[报告](../desktop/src/main/ai/report.ts)、[采集](../desktop/src/main/perception/screen-capture.ts)、[数据库](../desktop/src/main/db.ts)、[IPC](../desktop/src/main/ipc.ts)、[界面](../desktop/src/renderer/src/App.tsx)。

## 验证状态

本轮（T0）实际执行：`npm run typecheck` 通过；`npm run lint` 0 错误 0 警告；`npm test` 42 个测试全部通过（新增 5 个：预览零网络、空日预览、coverage 正确性、无 Key 预览警告、提示词禁比较断言）；`npm run build` 通过。

**未验证项**：真实模型返回的语义质量（需人工阅读输出）；渲染端确认/取消流程的界面行为（无自动化 UI 测试，需受控试用时手工复核）；真实 OCR 环境下的端到端流程。mock 测试不能证明模型从不胡乱比较，只能证明提示词约束已下发。

阶段结论：**待验收**（按 AGENTS.md 模板，见 [交接记录](handoffs/T0-2026-09-06.md)）。T0 通过只代表可进入受控原型试用，不代表洞察有价值。
