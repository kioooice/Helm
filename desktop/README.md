# Helm Desktop

桌面端应用（Electron + React + TypeScript + better-sqlite3）。

项目说明见仓库根目录的 [README.md](../README.md)，开发计划见 [PLAN.md](../PLAN.md)。

```bash
npm install
npm run dev    # 开发
npm test       # 测试（通过 Electron 自带的 Node 运行，保证 better-sqlite3 ABI 一致）
npm run build  # 构建
```
